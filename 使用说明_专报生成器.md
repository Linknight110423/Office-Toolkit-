# 专报文档生成器使用说明

这个小工具用于把 JSON 内容文件转换成标准专报 Word 文档。

当前推荐使用 Python 版界面：`Start_Zhuanbao_Python_GUI.cmd`。

## 使用方式

1. 双击 `Start_Zhuanbao_Python_GUI.cmd`。
2. 在窗口里选择 JSON 内容文件。
3. 选择 Word 模板。默认模板为 `专报格式模板.docx`，也可以选择其他模板。
4. 选择输出 Word 文件位置。
5. 点击“检查 JSON”。
6. 点击“生成 Word”。

## 模板设计器

主界面点击“模板设计器”，可以做这些事：

1. 从已有 Word 模板读取页面、标题、正文、表格等格式。
2. 修改页边距、页眉页脚距离。
3. 修改题目、引言、一级标题、二级标题、正文、表标题、数据来源、黑体强调的字体、字号、加粗、缩进、段前段后、行距。
4. 保存为新的 Word 模板。
5. 保存为 `.ps1` 或 `.json` 模板配置。
6. 重新加载 `.ps1` 或 `.json` 模板配置。
7. 把新模板设为主界面转换时使用的模板。

## 文件关系

- `zhuanbao.sample.json`：示例内容文件。
- `专报格式模板.docx`：标准格式模板。
- `zhuanbao_docx.py`：Python 版 Word 生成核心。
- `zhuanbao_template.py`：模板读取、模板配置、模板生成核心。
- `zhuanbao_gui.py`：Python 版交互界面。
- `Start_Zhuanbao_Python_GUI.cmd`：Python 版双击启动入口。
- `generate_zhuanbao_docx.ps1`：旧版 PowerShell 生成器。
- `ZhuanbaoGeneratorUI.ps1`：旧版 PowerShell 交互界面。
- `Start_Zhuanbao_Generator.cmd`：旧版双击启动入口。

## 注意事项

- 输出文件不要选成模板文件本身。
- 如果 Word 正在打开同名输出文件，可能会生成失败，先关闭对应 Word 文件再试。
- 工具会检查引言是否超过 250 字、全文是否超过 3000 字，并提醒英文缩写和表格数据来源。
- 若要制作新模板，建议从 `专报格式模板.docx` 复制一份后修改样式，这样程序能识别“专报题目、专报正文”等样式。
